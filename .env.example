# يجب أن يشير إلى نفس خادم الـ API الذي يستخدمه الـ backend (مثلاً http://localhost:4000).
# لوحة الإدارة واستعلامات الجداول تستخدم هذا العنوان؛ التسجيل يكتب في قاعدة بيانات ذلك الخادم فقط.
VITE_API_URL=http://localhost:4000
